import { Stack } from "@mui/material";
import Box from "@mui/material/Box";

const Home = () => {
  return (
    <Stack direction={`row`}>
      <Box></Box>
    </Stack>
  );
};

export default Home;
